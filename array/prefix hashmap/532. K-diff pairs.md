Need to deal with special case where K == 0

```cpp
int findPairs(vector<int>& nums, int k) {
        if (nums.empty() || k < 0) return 0;
        int res = 0;
        unordered_map<int,int>freq;
        for (int num : nums)
            freq[num]++;
        
        //edge case 
        if (k == 0){
            for (auto num: freq){
                if (num.second >= 2)
                    res+=1;
            }
            return res;
        }
        
        //normal case
        unordered_set<int>visited;
        for (int num : nums){
            if (visited.find(num) != visited.end()) continue; //skip nums already been used as the second num of the pair
            
            if (visited.find(num+k) != visited.end())
                res+=1;
            if (visited.find(num-k) != visited.end())
                res+=1;
         
            visited.insert(curr); 
        }
        return res;
    }
    ```
